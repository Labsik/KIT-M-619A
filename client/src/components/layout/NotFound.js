import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const NotFound = () => {
  return (
    <Fragment>
      <h1 className='text-primary'>Сторінка не знайдена</h1>
    </Fragment>
  );
};

NotFound.propTypes = {};

export default NotFound;
